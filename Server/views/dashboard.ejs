<head>
  <title>Ride</title> 
	<script src="http://code.jquery.com/jquery-2.0.0.min.js"></script>
	<link href='http://fonts.googleapis.com/css?family=Milonga' rel='stylesheet' type='text/css'>
	<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />	
	<style type="text/css">
		body {
    		padding: 10;
		    margin: 10;
		}
		html, body {
		    font: normal 14px 'courier new';
		}
		#content {
	    width: 500px;
	    float: left;
		}
		#screenshot {
	    margin-right: 60px;
  	  float: left;
		}
		path {  stroke: #fff; }
    path:hover {  opacity:0.9; }
    rect:hover {  fill:blue; }
    .axis {  
      font: 10px sans-serif;
    }
    .legend tr{    border-bottom:1px solid grey; }
    .legend tr:first-child{    border-top:1px solid grey; }

    .axis path,
    .axis line {
      fill: none;
      stroke: #000;
      shape-rendering: crispEdges;
    }

    .x.axis path {  display: none; }
	</style>	
	
</head>

<body>
	<div id="content">
  <h1>í ½íº² Ride Dashboard</h1>
  <table>
    <tr>
      <td>Total trips</td><td><%=bikeTripsCount%> trips</td>
    </tr>
    <tr>
      <td>Rated trips</td><td> <%= ratedTripsCount%> trips</td>
    </tr>        
    <tr>
      <td>Average Rating</td><td> <%=(totalRatingMagnitude/ratedTripsCount).toFixed(2)%></td>      
    </tr>
    <tr>
      <td>Total Distance</td><td> <%=(bikeTotalDistance / 1.60934).toFixed(1)%>m</td>      
    </tr>
    <tr>
      <td>Average Distance</td><td> <%=(bikeTotalDistance/(bikeTripsCount * 1.60934)).toFixed(1)%>m</td>      
    </tr>
  </table>
  <body>
  <div id='dashboard'>
  </div>
  

    
</div>
</body>
<script src="http://d3js.org/d3.v3.min.js"></script>
<script>
function dashboard(id, fData){
    var barColor = 'steelblue';
    var bar2Color = 'green';
    function segColor(c){ return {low:"#807dba", mid:"#e08214",high:"#41ab5d"}[c]; }
        
    // function to handle histogram.
    function histoGram(fD){
        var hG={},    hGDim = {t: 60, r: 0, b: 30, l: 0};
        hGDim.w = 2000 - hGDim.l - hGDim.r, 
        hGDim.h = 800 - hGDim.t - hGDim.b;
            
        //create svg for histogram.
        var hGsvg = d3.select(id).append("svg")
            .attr("width", hGDim.w + hGDim.l + hGDim.r)
            .attr("height", hGDim.h + hGDim.t + hGDim.b).append("g")
            .attr("transform", "translate(" + hGDim.l + "," + hGDim.t + ")");

        // create function for x-axis mapping.
        var x = d3.scale.ordinal().rangeRoundBands([0, hGDim.w], 0.1)
                .domain(fD.map(function(d) { return d[0]; }));

        // Add x-axis to the histogram svg.
        hGsvg.append("g").attr("class", "x axis")
            .attr("transform", "translate(0," + hGDim.h + ")")
            .call(d3.svg.axis().scale(x).orient("bottom"));

        // Create function for y-axis map.
        var y = d3.scale.linear().range([hGDim.h, 0])
                .domain([0, d3.max(fD, function(d) { return d[2]; })]);
                // Create function for y-axis map.
                
        var bars = hGsvg.selectAll(".bar").data(fD).enter()
                .append("g").attr("class", "bar");    
                
        //Weekends bar
        bars.append("rect")
            .attr("x", function(d) { return x(d[0]); })
            .attr("y", 0)
            .attr("width", x.rangeBand())
            .attr("height", hGDim.h)
            .attr('fill',function(d) { return (d[3] == 0 || d[3] == 6) ? "grey" : "white" });                       
                
        //Miles bar
        bars.append("rect")
            .attr("x", function(d) { return x(d[0]); })
            .attr("y", function(d) { return y(d[2]); })
            .attr("width", x.rangeBand())
            .attr("height", function(d) { return hGDim.h - y(d[2]); })
            .attr('fill',bar2Color);       
            
      //Miles bar label
      bars.append("text").text(function(d){ return d3.format(",")(d[2].toFixed(1))})
          .attr("x", function(d) { return x(d[0])+x.rangeBand()/2; })
          .attr("y", function(d) { return y(d[2])-5; })
          .attr("text-anchor", "middle");       
                     
        
        //Trips bar
        bars.append("rect")
            .attr("x", function(d) { return x(d[0]); })
            .attr("y", function(d) { return y(d[1]); })
            .attr("width", x.rangeBand())
            .attr("height", function(d) { return hGDim.h - y(d[1]); })
            .attr('fill',barColor);            
            
        //Trips bar label
        bars.append("text").text(function(d){ return d3.format(",")(d[1])})
            .attr("x", function(d) { return x(d[0])+x.rangeBand()/2; })
            .attr("y", function(d) { return y(d[1])-5; })
            .attr("text-anchor", "middle");
        return hG;
    }
    
    var hG = histoGram(fData); // create the histogram.
}
</script>

<script>
var weekData =  JSON.parse('<%-JSON.stringify(weekData)%>');
var graphData = Object.keys(weekData).map(function (key) { return weekData[key];}).sort(function(a,b) { return a[0] > b[0] ? 1 : a[0] < b[0] ? -1 : 0});
dashboard('#dashboard',graphData);
</script>